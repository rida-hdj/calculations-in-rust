# calculations

My first rust project :)

A simple CLI calculator written in Rust. Supports multiplication, addition, subtraction, and division.

## Run

```sh
cargo run --release
```

Or directly execute the binary:

```sh
./target/release/calculations
```
